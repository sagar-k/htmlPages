{
  "name": "sample",
  "version": "1.0.0",
  "description": "sample html pages",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm-run-all --serial scss",
    "scss": "node-sass --watch scss -o css"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.4.1",
    "sass": "^1.25.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "npm-sass": "^2.3.0"
  }
}
